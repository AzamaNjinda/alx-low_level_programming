#include <stdio.h>
/**
 * main - Write a program that prints a line using the funtion printf
 * Return: 0 (The program will return a zero to mean its successful)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
